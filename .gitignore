node_modules
dist
.DS_Store
.env
.env.*
coverage
server/ui-dist
growthub-local/instances
tmp
.npm-cache/
SKILL.md

# Agent worktrees and local settings (super admin only)
.claude/worktrees/
.claude/settings.json
.claude/settings.local.json
.claude/projects/
.claude/todos/

# Internal architecture snapshot (super admin only — not for public repo)
ARCHITECTURE.md

# Internal frozen knowledge snapshots (super admin only — not for public repo)
docs/FROZEN_*.md

# Observability snapshots (runtime data, not source)
.paperclip/instances/default/observability-snapshots/

# Local-only workspace artifacts
.worktrees/
.paperclip/config.json
.paperclip/config.json.backup
*.tsbuildinfo
