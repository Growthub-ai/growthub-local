/**
 * @paperclipai/adapter-opencode-local/ui — stub for open-source development.
 */
import type { TranscriptEntry, CreateConfigValues } from "@paperclipai/adapter-utils";

export function parseOpenCodeStdoutLine(line: string): TranscriptEntry | null {
  return null;
}

export function buildOpenCodeLocalConfig(_values: CreateConfigValues): Record<string, unknown> {
  return {};
}
