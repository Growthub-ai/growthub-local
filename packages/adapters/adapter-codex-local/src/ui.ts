/**
 * @paperclipai/adapter-codex-local/ui — stub for open-source development.
 */
import type { TranscriptEntry, CreateConfigValues } from "@paperclipai/adapter-utils";

export function parseCodexStdoutLine(line: string): TranscriptEntry | null {
  return null;
}

export function buildCodexLocalConfig(_values: CreateConfigValues): Record<string, unknown> {
  return {};
}
