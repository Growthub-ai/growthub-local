/**
 * @paperclipai/adapter-cursor-local — stub for open-source development.
 */
import type { AdapterModel } from "@paperclipai/adapter-utils";

export const agentConfigurationDoc = "Cursor local adapter configuration stub.";

export const models: AdapterModel[] = [
  { id: "cursor-default", label: "Cursor Default" },
];

export const DEFAULT_CURSOR_LOCAL_MODEL = "cursor-default";
