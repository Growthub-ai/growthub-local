/**
 * @paperclipai/adapter-openclaw-gateway/ui — stub for open-source development.
 */
import type { TranscriptEntry, CreateConfigValues } from "@paperclipai/adapter-utils";

export function parseOpenClawGatewayStdoutLine(line: string): TranscriptEntry | null {
  return null;
}

export function buildOpenClawGatewayConfig(_values: CreateConfigValues): Record<string, unknown> {
  return {};
}
