/**
 * @paperclipai/adapter-gemini-local/ui — stub for open-source development.
 */
import type { TranscriptEntry, CreateConfigValues } from "@paperclipai/adapter-utils";

export function parseGeminiStdoutLine(line: string): TranscriptEntry | null {
  return null;
}

export function buildGeminiLocalConfig(_values: CreateConfigValues): Record<string, unknown> {
  return {};
}
