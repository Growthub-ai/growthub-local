# Core HF / PEFT stack for SFT and light eval (CPU OK for small tests; GPU recommended).
# Keep versions aligned with [project.optional-dependencies] train in pyproject.toml.
huggingface_hub>=0.25.0
transformers>=4.46.0
accelerate>=1.0.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.11.0
numpy>=1.26.0
safetensors>=0.4.0
pyyaml>=6.0.1
