# Growthub Postiz Social Studio — kit-local notes (copy to .env if useful)
# Postiz itself uses its own .env in the cloned repo — see docs/postiz-fork-integration.md

# Default clone target for setup/clone-fork.sh and verify-env.mjs
POSTIZ_FORK_PATH=~/postiz-app

# Optional — AI-assisted caption ideation in exported plans (never commit real keys)
# OPENAI_API_KEY=
