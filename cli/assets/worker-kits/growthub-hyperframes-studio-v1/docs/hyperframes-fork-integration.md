# Hyperframes Fork Integration

Upstream repository:

- `https://github.com/heygen-com/hyperframes`

Primary execution flow:

1. Clone with `setup/clone-fork.sh`
2. Install and preview with Hyperframes CLI
3. Render deterministic outputs
