/**
 * Thin loader hook for future dynamic reference strategies (parity with resolver-loader).
 * Currently a no-op re-export so operators can extend the folder without route edits.
 */

export { collectReferenceOptions } from "./collect-reference-options.js";
