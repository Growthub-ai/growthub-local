/**
 * Dynamic Registry Pipeline — Type Definitions (re-export shim)
 *
 * Canonical definitions live in `@growthub/api-contract/pipelines`.
 */

export type {
  DynamicRegistryPipelineNode,
  DynamicRegistryPipeline,
  PipelineExecutionMode,
  PipelineValidationSeverity,
  PipelineValidationIssue,
  PipelineValidationResult,
  PipelineExecutionPackage,
  SerializedPipeline,
} from "@growthub/api-contract/pipelines";
